# R3M26 — model-based production convergence contract

Date: 2026-09-24. Authority: the user's explicit direction changes production from paper-author reproduction to an internally consistent, defensible atomic model, logically derived and numerically verified. Theory research precedes coding. This change does not retroactively pass old gates or authorize a new full collision in this web work unit.

Input: exact R3M25 `90d6cbad25e4bc49e9563f8721fdc408761b31c7`, tree `06021e67906649febec079668b6bbc977a2150b1`; all nine uploaded files match repository bytes. Frozen `cr_repro` numerical source remains `581ff84930bb862efafdfec5d39d5b25aebf9297ce7ce45f833a5abe125b2a5b`. Branch: `cr/r3m26-model-production-convergence-20260924`.

Model: nonrelativistic single electron, prescribed straight-line classical proton trajectory, fixed target, point-Coulomb two-center Hamiltonian. Dimensionful derivation is mapped explicitly to the established internal atomic units. CAP, finite box/start/stop, grid and finite hydrogen channel span are numerical approximations to the stated scattering observable, not excuses to omit their convergence. Model discrepancy against actual experiments remains separate from numerical error within this model.

Product objective remains independent all-bound capture at 50/100/225 keV/u and scoped downstream use. A validated 100-keV/u single-b finite-channel result is an intermediate product only; it neither replaces the all-bound/cross-section objective nor inherits its accuracy claim. Explicit total-capture/high-n/b-integral gates remain. AOCC is an optional independent comparison lane; completing an entire second solver is not a logical prerequisite if adequate independent operator/propagator/observable checks exist. No exact paper values, author raw files, source voting or digitized curve fit selects a production central.

Two evidence routes: (1) model-numerics validation using preregistered, held-out-checked conservative empirical error estimates plus cross-effect checks; (2) optional formal certification using rigorous bounds and controlled roundoff. Route (1) never reports a certified bound. Keep the existing numerical targets, including real-time .10%, spatial .30%, total 1%, unchanged. Prospective criteria may change under this user instruction; old decisions and evidence bytes are immutable. Clean p=2 is a diagnostic rather than a universal veto when a registered mixed-order model predicts unseen refinement data.

Theory deliverables: Hamiltonian/ETF/scalar gauge/projector/continuum vs finite-span derivation; CAP contraction and residual/global observable identities; unit/regime checks; source applicability and physical-model discrepancy; finite, prioritized closure route. Coding deliverables: prospective B3 forecast/evaluation with actual dt, held-out test and model sensitivity; typed empirical-versus-certified admission ledger and numerical theory checks. No speculative propagator replacement, production arrays, B3, preparation, finer h, b-grid or physical rate runs here.

Acceptance: preserve input hashes and scientific meanings; derive first, implement second; meaningful counterexample/analytic-limit tests; independently review only this changed theory/decision/code scope. A synthetic pass is not real B3 evidence. Current production remains HOLD until applicable dimensions actually close. R3M25 local independent-method evidence is reusable, not a new global certificate. One next canonical node remains B3 contract/time refinement after this closure policy is fixed; avoid another sequence of local-oracle certification nodes without new need.

Resources and delivery: CPU-only lightweight derivations/tests here; no CuPy/GPU claim. Local GPU planning uses actual R3M20/R3M25 inventory rather than old memory. Local model availability must not block research or cause silent routing; use Codex-only when an optional local model is unavailable. Existing authorized dedicated-branch publication and create-only Drive/Dropbox audit backup continue; main merge/force push and repeated large historical uploads remain out of scope.
